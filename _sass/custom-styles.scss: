body {
    background-color: black;
    color: white; // This will set the text color to white for better contrast against the black background.
    text-align: center; // This will center the content.
}

img.centered {
    display: block;
    margin-left: auto;
    margin-right: auto;
    max-width: 100%;
}

// Other custom styles can go here as needed.
